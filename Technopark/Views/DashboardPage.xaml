<Page x:Class="Technopark.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#F9FAFB">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24">

            <!-- Приветствие -->
            <TextBlock x:Name="WelcomeText" FontSize="14"
                       Foreground="#6B7280" Margin="0,0,0,20"/>

            <!-- Карточки статистики -->
            <UniformGrid x:Name="StatsGrid" Rows="1" Margin="0,0,0,28"/>

            <!-- Последние проекты -->
            <TextBlock Text="Последние проекты" FontSize="15"
                       FontWeight="SemiBold" Margin="0,0,0,10"/>
            <Border Background="White" CornerRadius="8"
                    BorderBrush="#E5E7EB" BorderThickness="1"
                    Margin="0,0,0,24">
                <DataGrid x:Name="ProjectsGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          RowHeight="42"
                          FontSize="13"
                          Background="White"
                          CanUserResizeRows="False"
                          SelectionChanged="ProjectsGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Направление" Binding="{Binding Direction.Name}" Width="130"/>
                        <DataGridTextColumn Header="Наставник" Binding="{Binding Mentor.FullName}" Width="160"/>
                        <DataGridTextColumn Header="Дата начала"
    Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding StatusDisplay}" Width="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Конкурсы -->
            <TextBlock Text="Ближайшие конкурсы" FontSize="15"
                       FontWeight="SemiBold" Margin="0,0,0,10"/>
            <Border Background="White" CornerRadius="8"
                    BorderBrush="#E5E7EB" BorderThickness="1">
                <DataGrid x:Name="ContestsGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          RowHeight="42"
                          FontSize="13"
                          Background="White"
                          CanUserResizeRows="False"
                          SelectionChanged="ContestsGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Конкурс" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Уровень" Binding="{Binding Level.Name}" Width="130"/>
                        <DataGridTextColumn Header="Дата" Binding="{Binding DateDisplay}" Width="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>